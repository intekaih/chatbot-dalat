{
  "name": "dalat-travel-chatbot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd ionic-tailwind-app && npm install && npx ng build --configuration production && cd ../server && npm install",
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev"
  }
}
